[DEFAULT]

; Projektspezifische Testkonfiguration fuer ECS LiPro + BMV.
; Keine Zugangsdaten oder Tokens in diese Datei eintragen.

LOGGING = INFO

DEVICES = /dev/serial/by-id/usb-FTDI_FT232R_USB_UART_AG0KG1HS-if00-port0

BMS_TYPE = EcsBmv
AUTO_DETECT_BMS = False
BLOCK_ON_DISCONNECT = True

MAX_BATTERY_CHARGE_CURRENT = 65
MAX_BATTERY_DISCHARGE_CURRENT = 130

SOC_CALCULATION = False

; Optional nach Laufzeittest:
; POLL_INTERVAL = 5000

; Den verwendeten ECS-RS485-Adapter hier NICHT ausschliessen.
; EXCLUDED_DEVICES =
